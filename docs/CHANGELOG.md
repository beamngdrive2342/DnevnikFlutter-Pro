# Changelog — DnevnikFlutter

Все значимые изменения в проекте. Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).

---

# Changelog — DnevnikFlutter

Все значимые изменения в проекте. Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).

---

## [Unreleased]

---

## [2026-06-25]

### Fixed
- Изображения домашних заданий (ДЗ) теперь сохраняются в оффлайн-кэш телефона (переписан `NetworkPhoto` с использованием `CachedNetworkImage`).
- Исправлена проблема с прыгающим скроллом при просмотре ГДЗ (`ListView` заменен на `SingleChildScrollView`).
- Оптимизирована загрузка ГДЗ: количество изображений для каждого номера теперь кэшируется локально, избегая лишних `HEAD` запросов к серверу при повторном открытии ответа.

### Removed
- **(Отменено)** Попытка внедрения push-уведомлений через FCM из-за сложностей настройки Vercel Node.js.
- **(Отменено)** Эксперимент с использованием `flutter_markdown` + `flutter_math_fork` + `flutter_svg` для рендеринга собственных решений ГДЗ. Создание геометрии с перспективой через сырой SVG код оказалось слишком неточным и было удалено.

### Reverted
- Экспериментальная интеграция Push-уведомлений (FCM) и Vercel-бэкенда для их рассылки. Все изменения отменены из-за сложностей настройки серверного окружения на Vercel (ошибка импортов ESM/CommonJS).

---

## [2026-06-24]

### Added
- Автоматическая подгрузка готовых ответов (ГДЗ) по Геометрии и Алгебре при просмотре домашнего задания.
- Внедрение ИИ-распознавания фотографий с доски при быстром добавлении домашнего задания (Gemini Vision API). ИИ научился находить номера задач на фото и связывать их с предметом.
- На экране ученика (`LessonCard`) добавлена кнопка "Открыть ответы", которая вызывает непрозрачное модальное окно с фотографиями решений (фотографии загружаются из сети).

### Changed
- В модель `HomeworkItem` добавлено поле `textbookNumbers`.
- Обновлен системный prompt ИИ в `ai_service.dart`: теперь он игнорирует скобки и буквы при парсинге номеров из текста/фото.
- **Миграция ресурсов:** Готовые ответы (около 3400 картинок по Геометрии и Алгебре) перенесены в публичный репозиторий GitHub.
- Изображения теперь загружаются через `cached_network_image` для обеспечения надежного офлайн-кеширования.

### Fixed
- **Офлайн режим:** Исправлен баг, из-за которого происходил разлогин при запуске приложения без интернета. Добавлено локальное сохранение документа класса в `SharedPreferences`.
- **Офлайн режим:** Исправлено отображение нескольких фотографий решения в офлайне (теперь количество фото для каждого номера кешируется локально, что позволяет `cached_network_image` выводить их без сетевых `http.head` проверок).

---

## [2026-06-23]

### Added
- Базовый виджет `PremiumCard` (эффект глассморфизма, встроенная анимация нажатия).
- Глобальный лоадер-скелетон (`shimmer_loading.dart`) для сглаживания UI при загрузке расписания.
- Эффект свайпа вверх для входа на `welcome_screen.dart` (паттерн Swipe-up to Enter) с плавным BottomSheet модальным окном.
- Кастомный мягкий `Pull-to-Refresh` для обновления списка расписания и ДЗ в `diary_screen.dart` с использованием кастомной анимации `PremiumRefreshControl`.
- Добавлена кастомная иконка приложения через пакет `flutter_launcher_icons` (в премиальном стиле Sahara Warm Minimalism).
- **Все системные Material иконки заменены на премиальный пак `lucide_icons`** (в главном меню, карточках уроков и на стартовом экране).
- **Добавлены "живые" микро-анимации на нажатия** (ScaleTapWrapper) для интерактивных элементов (календарь, кнопки в верхней панели, переключатель темы) для создания тактильного WOW-эффекта.
- Улучшена логика ленты календаря: точка под датой теперь показывает наличие **домашнего задания** (а не просто уроков), а выходные дни подсвечиваются цветом (при этом суббота перестает быть выходным, если в расписании есть уроки).
- В карточке урока добавлена визуальная подсказка-иконка (изображение), если к домашнему заданию прикреплено фото, чтобы ученику не приходилось полностью открывать задание.
- Исправлена цветовая палитра тумблера смены темы: теперь он органично сливается с фоном (`surface3`) и выглядит естественно как в светлом, так и в тёмном режимах.

### Changed
- `LessonCard` полностью переписан на основе `PremiumCard` с микро-анимацией раскрытия (AnimatedSize).
- Позиция стрелочки раскрытия в `LessonCard` изменена (перемещена к времени урока) во избежание перекрытия FAB.
- Исправлен контраст карточек `PremiumCard` и `SkeletonLessonCard` в светлой теме (увеличена непрозрачность фона, добавлена видимая обводка).

---

## [2026-06-22]

### Changed
- Редизайн экрана входа (Welcome Screen): применен новый минималистичный дизайн с радиальным свечением (RadialGradient) и строгими темными кнопками-плашками в оригинальных цветах.

---

## [2026-06-21]

### Security
- Gemini API-ключ вынесен на Vercel-прокси — клиент больше не хранит ключ напрямую
- Улучшена система авторизации Firebase Auth: токены хранятся в `flutter_secure_storage`

---

## [2026-06-18]

### Added
- Оффлайн-кэш домашних заданий: память (TTL 20 сек) + диск (`SharedPreferences`)
- Фоновое обновление ДЗ без блокировки UI (`_refreshHomeworkInBackground`)
- Автоматическая очистка устаревших ДЗ (старше 14 дней) через `_purgeExpiredHomework`

### Changed
- `FirestoreService.getHomework` — добавлены три уровня кэша: memory → disk → network

---

## [2026-06-15]

### Added
- Система классов: создание класса (`create_class_screen.dart`) и вступление по коду (`join_class_screen.dart`)
- Изоляция данных по `classId` — расписание и ДЗ отдельны для каждого класса
- Панель администратора (`admin_panel_screen.dart`) для управления расписанием класса

### Changed
- Firestore-коллекция ДЗ перенесена с `/homework` на `/classes/{classId}/homework`

---

## [2026-06-12]

### Added
- Модели данных `Lesson` и `HomeworkItem` через `freezed` + `json_serializable`
- `ClassSchedule` — статический синглтон для хранения расписания класса
- Цветовая маркировка предметов (`subjectColors`) для визуального различия

---

## [2026-06-10]

### Added
- Навигация через `go_router ^17.3.0`
- `auth_gate.dart` — автоматический роутинг в зависимости от состояния авторизации

---

## [2026-06-08]

### Added
- State management через `flutter_riverpod ^3.3.2`
- `auth_provider.dart` — провайдер состояния авторизации

---

## [2026-06-05]

### Added
- `ai_service.dart` — интеграция с Google Gemini API через Vercel-прокси
- AI-чат с контекстом расписания и ДЗ (`getAIResponse`)
- Умный парсинг ДЗ из текста (`recognizeQuickHomework`): «Алгебра на завтра номера 45 46» → структурированный объект
- Поддержка изображений в чате (base64 + mime type)

---

## [2026-06-01]

### Added
- Инициализация Flutter-проекта `dnevnik_app`
- `firestore_service.dart` — CRUD домашних заданий через Firestore REST API (без FlutterFire)
- `auth_service.dart` — Firebase Auth через REST API
- `diary_screen.dart` — экран дневника / списка ДЗ
- `add_homework_modal.dart` — модалка добавления ДЗ (вручную + AI-парсинг)
- `welcome_screen.dart` — экран онбординга
- `main_screen.dart` — главный экран с таб-навигацией
- Базовая тема и стили (`theme/`)

---

**Формат секций:** `Added` — новое, `Changed` — изменения, `Fixed` — исправления, `Removed` — удалённое, `Security` — безопасность
# #   [ 2 0 2 6 - 0 6 - 2 3 ] 
 # # #   C h a n g e d 
 -   R e m o v e d   ' H o m e w o r k   D e t a i l s '   b o t t o m   s h e e t   ( \ _ s h o w L e s s o n D e t a i l s S h e e t \ ) . 
 -   H o m e w o r k   i m a g e s   a r e   n o w   d i s p l a y e d   i n l i n e   w i t h i n   t h e   e x p a n d e d   \ L e s s o n C a r d \   a s   a   h o r i z o n t a l   s c r o l l a b l e   s t r i p . 
 -   T a p p i n g   i n l i n e   i m a g e s   s m o o t h l y   a n i m a t e s   t h e m   t o   a   f u l l - s c r e e n   v i e w   u s i n g   \ H e r o \   t r a n s i t i o n s . 
 -   F u l l y   c l e a n e d   u p   u n u s e d   m e t h o d s   i n   \ d i a r y _ s c r e e n . d a r t \ . 
  
 